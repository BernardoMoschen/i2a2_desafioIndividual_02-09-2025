This file was intentionally cleared.

Container orchestration artifacts have been removed from the repository to simplify deployment and local development without container tooling.

If you need the original file, restore it from the git history.
ENV PYTHONUNBUFFERED=1 \
